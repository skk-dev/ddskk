-*- text -*-
             $Date: 1999/09/15 11:44:20 $ Mikio Nakajima/中島幹夫 <minakaji@osaka.email.ne.jp>
                                                         　　　　 <minakaji@pdx.ne.jp> 
	     $Id: INSTALL,v 1.5 1999/09/15 11:44:20 minakaji Exp $ 

(1)Get APEL 9.22 or later and install it.  You can get latest APEL at;

    ftp://ftp.m17n.org/pub/mule/apel/
or
    ftp://ftp.etl.go.jp/pub/mule/apel/

(2)If you use Emacs 19 with mule feature, you may want custom package.
   If you don't have it, APEL provides dummy functions.

   (a)On GNU Emacs 20 or XEmacs, SKK should work correctly without any
      modifications or additional packages.

   (b)On GNU Emacs 19.30 or later has custom package by default, but
      you can get better version (custom-1.9962.tar.gz) at

        ftp://ftp.dina.kvl.dk/pub/Staff/Per.Abrahamsen/custom/

      Apply patch skk-10/patch/custom-1.9962.patch to
      cus-face.el of the package.

   (c)On GNU Emacs 19.28, SKK does not work with any custom package.
      You should use tinycustom.el of APEL for an emulation purpose.
      By default, SKK requires pcumstom.el which automatically defines
      to use actual custom package or tinycustom.el, and will define
      to use tinycustom.el for GNU Emacs 19.28.  So you have to do
      nothing.  After load tinycustom.el, all `defcustom's in SKK
      package will be equal to `defvars'.

(3)Edit SKK-CFS as you like.  Maybe you do not need edit Makefile at
   all.  All customization can be done in SKK-CFG.

(4)Run `make install' (You may have to be superuser).  Or you may want
   to run `make install-package' if you use XEmacs (package install).
  You can specify Emacs command name in a command line.  For examples,
  `make install EMACS=mule'

(5)Many variables/functions names have been changed.  You are strongly
   recommended to load skk-obsolete.el and

     M-x skk-obsolete-check-all-files.

Mikio Nakajima/中島幹夫 <minakaji@osaka.email.ne.jp>
               　　　　 <minakaji@pdx.ne.jp>
http://www.asahi-net.or.jp/~gy2m-nkjm/
