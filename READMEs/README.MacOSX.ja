-*- outline -*-$Date: 2007/03/31 16:39:19 $$Id: README.MacOSX.ja,v 1.4 2007/03/31 16:39:19 skk-cvs Exp $Mac OS X で SKK を使う方法について説明します。同梱の INSTALL および README にも目を通しておいて下さい。* 事前準備** Developer ToolsのインストールMac OS Xは初期状態ではmakeコマンドがインストールされていません。以後の作業をするためにはDeveloper Toolsをインストールする必要があります。Developer ToolsはOSをパッケージで購入した場合は同梱されています。もし最新のDeveloper Toolsが必要な場合はApple Developer Connection(https://connect.apple.com/)にて無料の会員登録をする事でダウンロードにかかるコストのみで入手する事ができます。** コンパイルに利用するEmacsの確認TerminalよりEmacsの位置を確認します。$ which emacsMac OS X標準のEmacs は `/usr/bin/emacs' に存在します。Carbon Emacs(http://members.shaw.ca/akochoi-emacs/)はconfigureオプションを付けづにインストールした場合は`/usr/local/bin/emacs' に存在します。初期状態ではwhichコマンドにより返されたEmacs の標準LISPDIRにインストールされます。もしインストール対象のEmacsを変更する場合は.tcshrcや.basrc等にて環境変数PATHの設定を変更してください。* Emacs** APEL のインストールまず APELをインストールする必要があります。Daredevil SKK では、APEL10.7 以降での動作が確認されていますが、APEL は Emacs/XEmacs のバージョンアップに合わせて日進月歩なので、できるだけ新しいバージョンをインストールすることをお勧めします。*** APEL の 取得APEL は、     http://kanji.zinbun.kyoto-u.ac.jp/%7Etomo/lemi/dist/apel/     ftp://ftp.jpl.org/pub/elisp/apel/などから入手できます。*** APEL の展開(解凍)APELのアーカイブをダブルクリックすればStuffIt Expanderにより解凍されます。Terminalの操作になれている場合は以下のコマンドを実行する事で解凍できます。(xはバージョン番号です)$ tar xvfz apel-10.x.tar.gz*** APEL の byte-compileとインストールAPELの展開ディレクトリでTerminalより以下のコマンドを実行します。$ make$ sudo make install$ password: (Passwordを聞いてくるので管理パスワードを入力)*** load-path への追加whichコマンドで返って来たEmacsを利用する場合は設定の必要はありません。** SKKのインストール*** SKK の展開(解凍)APELと同様の方法で解凍できます。*** SKK の byte-compileとインストールAPELと同様の方法でインストールできます。*** load-path への追加whichコマンドで返って来たEmacsを利用する場合は設定の必要はありません。*** .emacs への追加whichコマンドで返って来たEmacsを利用する場合は設定の必要はありません。* Mac OS X 上で動く辞書サーバについてMac OS X で利用できる SKK 辞書サーバには次のようなものがあります。** skkservskkserv は openlab の cvs repositry から入手できます。  $ cvs -d :pserver:guest@openlab.ring.gr.jp:/circus/cvsroot checkout skk/skkserv** dbskkddbskkd は http://www.ne.jp/asahi/bdx/info/software/jp-dbskkd.html から入手できます。** script 言語で実装されたサーバperl, ruby が使用できるならば、pskkserve や rskkserve が使用できます。pskkserv はftp://skk.kuis.kyoto-u.ac.jp:/pub/skk/9.6/plain/contrib/pskkservrskkserve はhttp://www.ruby-lang.org/en/raa-list.rhtml?name=rskkservから入手できます。* SKK on Mac OS XMac OS X 上で動作する SKK ライクの IME があります。** Aqua SKKCocoaにより実装されています。SKK と共通の辞書が使用できます。http://airscope.netfirms.com/cgi-bin/dita/dita.cgi/aquaskk* 他の情報源http://sakito.s2.xrea.com/detail/emacs/emacs21.html[end of README.MacOSX]